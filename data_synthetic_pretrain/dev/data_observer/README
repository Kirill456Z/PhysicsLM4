### Data Observability UI

#### Web UI tool for examining generated data from data_synthetic_pretrain_package

Given a specified data generation and formatting config the tool generates SyntheticTasks from the package
The tool allows to observe the graph by displaying it in the ui, the context and the answers generated by the model per task
